# USER_DOC — プレイヤー／評価者向けマニュアル

このドキュメントは、cub3D の遊び方と、自作マップ（`.cub` ファイル）を作成するためのルールをまとめたものです。**実装と一致する内容のみ** を記載しています。

---

## 1. 起動

```
make
./cub3D <path/to/map.cub>
```

`maps/valid/` 配下に動作確認用のマップが用意されています。

```
./cub3D maps/valid/1.cub
./cub3D maps/valid/2.cub
./cub3D maps/valid/enemy_line.cub
```

## 2. 操作

| キー | 動作 |
|---|---|
| `W` / `S` | 前進 / 後退 |
| `A` / `D` | 左右への平行移動（ストレイフ） |
| `←` / `→` | 視点を左右に回転 |
| `1` | ピストルを装備 |
| `2` | フラッシュライト（懐中電灯）を装備 |
| `3` | 素手（移動が 1.3 倍速くなる） |
| `Space` | 射撃（ピストル装備時のみ。クールダウンあり） |
| `I` | UI（ミニマップ・収集進捗）の表示切替 |
| `O` | クロスヘアの表示切替 |
| `L` | 距離による陰影（シェード）の切替 |
| `Esc` / ウィンドウの × | 終了 |

> 移動は WASD、回転は左右矢印のみです。上下矢印・Q・E は割り当てられていません。

## 3. ゲーム要素

- **収集アイテム**（マップ文字 `4` または `k`〜`o`、`.cub` の `OC*` テクスチャ）に触れると獲得され、UI に進捗（`Collect: x / y`）が表示されます。すべて集めると `GG !` の表示になります。収集対象が 1 つもないマップでは `Nothing to collect !` と表示されます。
- **敵**（マップ文字 `M`）はプレイヤーの方角に応じた 8 方向スプライトで描画されます。普段は巡回し、**正面の一定範囲（視界）にプレイヤーが入り、間に遮蔽物（壁・通行不可オブジェクト）がない** と検知して追跡へ切り替わります。見失っても一定時間（既定 5 秒、`.cub` の `ET` で変更可）は追跡を続け、時間切れで巡回へ戻ります。ピストルで撃って既定 10 ヒット（`.cub` の `EH` で変更可）させると倒せます。移動速度は `.cub` の `ES` で調整できます（プレイヤーの移動速度とは独立）。
- **巡回路**（マップ文字 `P`）は敵が巡回するための通路です。`P` セルをループ状に並べておくと、敵はその上を周回します。敵が `P` 上にいないときは、最も近い `P` へ自動で復帰します。`P` を 1 つも置かなければ、敵は検知時のみ動き、それ以外は待機します。
- **通行不可オブジェクト**（`2` または `a`〜`e`、`OI*`）は壁と同様に進行を妨げます。
- **通行可オブジェクト**（`3` または `f`〜`j`、`OP*`）は装飾として描画されますが、通り抜けられます。フラッシュライト的に周囲をわずかに明るくする効果があります。

## 4. `.cub` ファイルの記述ルール

### 4.1 設定ヘッダ

ファイル前半に、解像度・テクスチャ・色・各種パラメータを記述します。同じキーは **1 度だけ** 記述できます。記述順は自由ですが、**マップ本体より前** に置く必要があります。

```
R 1920 1080                                # 解像度 (幅 高さ)

NO ./textures/wall/Wall_1.xpm              # 北向きの壁テクスチャ
SO ./textures/wall/Wall_2.xpm              # 南向きの壁テクスチャ
WE ./textures/wall/Wall_3.xpm              # 西向きの壁テクスチャ
EA ./textures/wall/Wall_4.xpm              # 東向きの壁テクスチャ

ST ./textures/wall/Wall_mossy.xpm          # 天井テクスチャ (任意)
FT ./textures/wall/Wall_tile.xpm           # 床テクスチャ (任意)

F 184,113,39                               # 床の色 R,G,B (テクスチャ未指定時のフォールバック)
C 51,198,227                               # 天井の色 R,G,B (テクスチャ未指定時のフォールバック)

OI1 ./textures/object/Object_barrel.xpm    # 通行不可オブジェクト 1種目 (マップ文字 '2' / 'a')
OP1 ./textures/enemy/Enemy_1.xpm           # 通行可オブジェクト   1種目 (マップ文字 '3' / 'f')
OC1 ./textures/object/Object_star.xpm      # 収集アイテム         1種目 (マップ文字 '4' / 'k')

MS 0.05                                     # 移動速度     (任意, 0 より大。既定 0.05)
RS 0.05                                     # 回転速度     (任意, 0 より大。既定 0.05)
FOV 0.66                                    # 視野角       (任意, 大きいほど広角。既定 0.66)
ET 5.0                                      # 敵の追跡秒数 (任意, 見失うまでの秒数。既定 5.0)
ES 0.11                                     # 敵の移動速度 (任意, 0 より大。既定 0.11, 移動速度と独立)
EH 10                                       # 敵のHP       (任意, 0 より大の整数。撃破に要するヒット数。既定 10)
```

サポートされる画像形式は `.xpm` のみです。

#### 設定キー一覧

| キー | 内容 | 必須 |
|---|---|---|
| `R` | 解像度（幅 高さ） | 必須 |
| `NO` / `SO` / `WE` / `EA` | 各方角の壁テクスチャ | 必須 |
| `ST` | 天井テクスチャ | 任意（未指定時は `C` の色） |
| `FT` | 床テクスチャ | 任意（未指定時は `F` の色） |
| `F` / `C` | 床 / 天井のフォールバック色 `R,G,B` | テクスチャ未指定時に使用 |
| `OI1`〜`OI5` | 通行不可オブジェクトのテクスチャ（最大 5 種） | 任意 |
| `OP1`〜`OP5` | 通行可オブジェクトのテクスチャ（最大 5 種） | 任意 |
| `OC1`〜`OC5` | 収集アイテムのテクスチャ（最大 5 種） | 任意 |
| `MS` / `RS` / `FOV` | 移動速度 / 回転速度 / 視野角 | 任意（既定値で動作） |
| `ET` / `ES` / `EH` | 敵の追跡秒数 / 移動速度 / HP | 任意（既定値で動作） |

> **後方互換に関する注意:** 旧仕様の裸キー `OI` / `OP` / `OC`（番号なし）は **現在サポートされていません**。番号付きの `OI1` / `OP1` / `OC1` を使用してください。一方、マップ本体の文字 `2` / `3` / `4` は各カテゴリ 1 種目（`a` / `f` / `k`）の別名として **引き続き使用できます**。

### 4.2 マップ本体

ヘッダの後にマップを記述します。マップは `.cub` の **最後** に置く必要があります。各セルはスペースで区切ります（例: `1 0 0 1`）。

使用可能な文字は以下のとおりです。

| 文字 | 意味 | 対応テクスチャ |
|---|---|---|
| `0` | 通行可能な床 | — |
| `1` | 壁（通行不可） | `NO`/`SO`/`WE`/`EA` |
| `2` または `a`〜`e` | 通行不可オブジェクト（5 種） | `OI1`〜`OI5` |
| `3` または `f`〜`j` | 通行可オブジェクト（5 種, 通り抜け可） | `OP1`〜`OP5` |
| `4` または `k`〜`o` | 収集アイテム（5 種, 触れると獲得） | `OC1`〜`OC5` |
| `M` | 敵の出現地点 | 敵テクスチャ（8 方向, 内蔵） |
| `P` | 巡回路（敵が周回する通路。床として通行可能） | —（描画は床扱い） |
| `N` / `S` / `E` / `W` | プレイヤー初期位置と向き | — |
| 空白 | 描画されない領域 | — |

オブジェクトの文字とテクスチャ番号の対応:

- 通行不可: `2`=`a`→`OI1`, `b`→`OI2`, `c`→`OI3`, `d`→`OI4`, `e`→`OI5`
- 通行可　: `3`=`f`→`OP1`, `g`→`OP2`, `h`→`OP3`, `i`→`OP4`, `j`→`OP5`
- 収集　　: `4`=`k`→`OC1`, `l`→`OC2`, `m`→`OC3`, `n`→`OC4`, `o`→`OC5`

ルール:

1. プレイヤーのスタート地点 `N` / `S` / `E` / `W` は **ちょうど 1 つ** 必要です。
2. マップは壁 `1` で囲まれている必要があります。隙間や開いた境界はパースエラーになります。
3. 字句解析後、すべての行の列数が一致している必要があります（空白を除く）。
4. 使用したオブジェクト文字に対応するテクスチャキー（例: `b` を使うなら `OI2`）を定義してください。

> **巡回路の設計ヒント:** `P` セルを **閉じたループ状** に並べると、敵が安定して周回します。敵（`M`）は `P` の上、または `P` に隣接させて配置すると、巡回にスムーズに乗ります。分岐や行き止まりのある `P` でも動作しますが、ループ状が最も意図どおりになります。`P` を置かないマップでは、敵は検知時のみプレイヤーを追跡し、それ以外は待機します。

### 4.3 解像度の制約

実行時、要求された解像度は内部で次の範囲にクリップされます。

- 最小: 848 × 480
- 最大: 1920 × 1080

これより大きな値を書いても、自動的に 1920 × 1080 まで縮められます。アスペクト比に応じて FOV が内部補正されます。

## 5. うまく動かないとき

- **`Error: invalid map.`** … `.cub` の構文か、マップの壁が閉じていない可能性があります。裸キー `OI`/`OP`/`OC` を使っていないか、使用したオブジェクト文字に対応する `OI*`/`OP*`/`OC*` を定義し忘れていないかも確認してください。
- **`Error: failed to load texture(s).`** … テクスチャパスは `.cub` ファイルからの相対ではなく、**`cub3D` を起動するカレントディレクトリから** の相対で解決されます（例: リポジトリルートで `./cub3D maps/valid/1.cub` を実行する想定）。
- **画面が真っ黒** … テクスチャパスが間違っている、または XPM 以外の画像形式を指定している可能性があります。
- **敵が巡回しない／その場で止まる** … マップに `P`（巡回路）が無い、または敵が `P` から離れすぎている可能性があります。`P` をループ状に配置し、敵をその近くに置いてください。

> パースに失敗した場合、現状の実装では原因切り分け用に `DEBUG: ...` という行が標準出力に出ることがあります（開発用の出力です）。

## 6. デバッグ用スクリーンショット（実験的・未配線）

`codes/srcs/core/bmp.c` に BMP 出力機能（`screenshot()` / `save_bmp()`）が実装されていますが、**現在のところメインフローからは呼び出されておらず、キーバインドもコマンドラインオプションも公開されていません**。`FLAG_SAVE` フラグは定義のみで未使用です。利用するには手動で `screenshot()` を呼ぶ導線を追加する必要があります（既知の課題は DEV_DOC を参照）。
