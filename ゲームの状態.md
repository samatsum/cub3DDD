# cub3D Architecture Specification

## 1. 現状のシステム概要 (Current System Overview)
本プロジェクトは、C言語およびMinilibX（X11）を用いて構築された、レイキャスティング（DDAアルゴリズム）ベースの3D迷路ゲームエンジンである。
Linux環境に特化して最適化されており、将来的な機能拡張（マルチプレイや動的エンティティの追加）を前提とした、疎結合かつ拡張性の高いモジュール設計が採用されている。

## 2. ディレクトリ・モジュール構造の詳細 (Detailed Directory Structure)
ソースコード（処理）とヘッダー（定義・インターフェース）が完全に分離され、さらに各モジュールが「単一責任の原則」に従ってファイル単位で細分化されている。

    cub3ddd/
    ├── Makefile            # Linux(X11)環境専用のビルドスクリプト
    ├── codes/
    │   ├── includes/       # ヘッダーファイル群 (.h) - マクロ定数とインターフェース
    │   │   ├── config/     # config.h: 画面解像度制限などのシステム設定値
    │   │   ├── core/       # core.h: ゲームループ、中核となるフラグ定義
    │   │   ├── engine/     # raycast.h, render.h, texture.h 等: 演算・描画用マクロ
    │   │   ├── gnl/        # get_next_line.h: ファイル読み込み用プロトタイプ
    │   │   ├── ui/         # ui.h: UIの座標、色、サイズなどのマクロ定義
    │   │   ├── utils/      # utils.h: 汎用関数群のプロトタイプ
    │   │   ├── cub3d.h     # 統括ヘッダー（各ファイルでのインクルードを最小化）
    │   │   └── types.h     # 全構造体 (t_window, t_config, t_render等) の一元定義
    │   │
    │   └── srcs/           # ソースコード群 (.c) - 内部実装
    │       ├── main.c      # エントリーポイント (引数解析、初期化起動、BMP出力分岐)
    │       │
    │       ├── config/     # 設定・マップ解析モジュール
    │       │   ├── config.c         # 設定データ構造体の初期化とエラー時の解放
    │       │   ├── parse_map.c      # .cubファイル後半からのマップ(2次元配列)構築
    │       │   ├── check_map.c      # マップが壁(1)で囲まれているかのバリデーション
    │       │   ├── parse_params.c   # 床/天井のRGB値、解像度パラメーターの抽出
    │       │   └── parse_texture.c  # 壁(NO/SO/WE/EA)・スプライト画像のパス抽出
    │       │
    │       ├── core/       # ゲームシステム中核モジュール
    │       │   ├── init.c           # ウィンドウ生成とMinilibX初期変数のセットアップ
    │       │   ├── loop.c           # メインループ (入力検知・物理演算・描画更新の統括)
    │       │   ├── exit.c           # メモリとウィンドウの完全な解放、安全な終了処理
    │       │   ├── bmp.c            # [-save]起動時の初フレーム画像(BMP)生成・保存機能
    │       │   └── item.c           # (将来拡張用) アイテム取得・管理ロジック
    │       │
    │       ├── engine/     # 物理演算・描画エンジンモジュール
    │       │   ├── input/input.c    # キーボード/マウスイベントのフックと状態保存
    │       │   ├── raycast/         # レイキャスト演算 (DDA)
    │       │   │   ├── raycast.c    # DDA本体 (壁との交点と距離のベクトル計算)
    │       │   │   ├── camera.c     # プレイヤー座標(歩行)と視点ベクトル(回転)の更新
    │       │   │   └── tables.c     # 三角関数や描画比率の事前計算 (処理落ち防止キャッシュ)
    │       │   ├── render/          # グラフィック描画処理
    │       │   │   ├── screen.c     # バッファクリアとウィンドウへの画像一括転送
    │       │   │   ├── draw.c       # ピクセル描画、直線・矩形を描く低レイヤー関数
    │       │   │   ├── draw_wall.c  # 交点距離に基づくテクスチャの動的スケーリング・垂直描画
    │       │   │   ├── draw_sky_floor.c # 天井と床の分割描画
    │       │   │   └── sprite.c等   # スプライト(物体)の距離ソートと壁判定描画
    │       │   └── texture/         # 画像解析
    │       │       ├── texture.c    # .xpm画像ファイルから内部データ構造体への変換
    │       │       └── color.c      # ピクセル色の取得、距離に応じた暗転(シャドウ)処理
    │       │
    │       ├── gnl/        # ファイル読み込み処理
    │       │   └── get_next_line.c  # fdから安全にテキストを1行ずつ読み込む標準関数
    │       │
    │       ├── ui/         # ユーザーインターフェース (HUD) モジュール
    │       │   ├── ui.c             # 画面端のミニマップ、進行度などのテキスト描画
    │       │   ├── crosshair.c      # 画面中央の照準線(クロスヘア)の描画
    │       │   └── shortcuts.c      # UIや影のON/OFFを切り替えるショートカット操作
    │       │
    │       └── utils/      # 独自標準ライブラリ (Libft拡張)
    │           ├── ft_*.c           # atoi, split, strlen等の自作C言語標準関数群
    │           ├── pos.c            # 座標(X,Y)構造体を操作するためのショートカット関数
    │           └── str.c            # 独自の文字列操作ユーティリティ
    │
    ├── minilibx-linux/     # Linux用グラフィックライブラリ (X11)
    ├── maps/               # 動作テスト用の .cub マップデータ群
    └── textures/           # 壁やスプライトに使用する .xpm / .png テクスチャ画像群

## 3. 現在のコードの良い点 (Strengths / Good Points)

### ① 単一責任原則に基づいたデータ構造
`t_window`, `t_config`, `t_camera`, `t_render` といった機能単位で構造体が細分化されている。各モジュールには必要最小限の構造体のみが渡されるため、関数の影響範囲が明確であり、意図しないデータの書き換えが起きない安全な設計となっている。

### ② 設定値とロジックの完全な分離（マジックナンバー排除）
移動速度、UIのサイズ、各種カラーコード、解像度の限界値といった「設定値」が、ソースコード（`.c`）内に直接記述されていない。すべて `includes/` 配下の機能別ヘッダーファイルでマクロとして一元管理されているため、コードを読まずともゲームバランスやデザインの調整が可能である。

### ③ 環境最適化と完全なメモリ管理
Linux (X11) 環境に完全に特化しており、OS固有のウィンドウ・ディスプレイ破棄処理（`mlx_destroy_display`等）が適正なライフサイクルで実行される。Valgrindを用いたテストにおいて、正常系・異常系ともにメモリリークおよび未初期化バグゼロ（`All heap blocks were freed`）が保証されている。

### ④ テクスチャの動的スケーリング
壁やスプライトのレンダリングにおいて、特定の画像サイズ（例: 64x64）に依存する計算式が存在しない。ロードした画像データから動的に縦横サイズを取得してDDAのテクスチャ座標を計算するため、任意の解像度のテクスチャを無改造で利用できる。

### ⑤ 実用的なスクリーンショット出力機能（デバッグ補助）
起動引数に `-save` を指定することで、最初の1フレームを無劣化のBMP画像として保存し、即座に終了する機能（`core/bmp.c`）を備えている。画像はタイムスタンプ付き（`screenshot_YYYY_MMDD_HHMM.bmp`）で自動的に `~/bmp/` ディレクトリに保存される仕様となっており、描画デバッグに極めて有用である。

---


---

## 4. 開発の始め方 (Getting Started)

    # リポジトリのクローン後、コンパイルを実行
    make

    # ゲームの実行（任意のマップファイルを指定）
    ./cub3D maps/1.cub

    # メモリリークテスト（完全なクリーンアップの確認）
    valgrind --leak-check=full ./cub3D maps/1.cub

    # スクリーンショット出力（~/bmp/ 内に画像を生成して即座に終了）
    ./cub3D -save maps/1.cub